This directory contains the archived backend files that were removed from the main library surface.

Files moved here:
- src/index.ts (Fastify server entrypoint)
- src/routes/* (HTTP route handlers)
- docker-compose.yml

Purpose: Keep a copy in case the HTTP server is needed later. This folder is not built into the library exports by default.